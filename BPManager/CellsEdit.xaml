<Window x:Class="BPManager.CellsEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BPManager"
        mc:Ignorable="d"
        Title="Edit Cells" Height="470" Width="300"
        WindowStartupLocation="CenterScreen"
        >
    <Grid Background="#FF807C7C">
        <StackPanel HorizontalAlignment="Center">
            <ListBox x:Name="comboCellsList" Width="200" Height="300" Margin="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CellTitle}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button x:Name="buttonDelete" Content="Delete" Width="200" Height="25" Margin="10,0,10,10" Click="buttonDelete_Click"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBox x:Name="textNewCellName"  Width="145"/>
                <Button  x:Name="buttonAdd"  Content="Add" Width="50" Margin="5" Height="25" Click="buttonAdd_Click"/>
            </StackPanel>
            <Button x:Name="buttonClose" Content="Close" Width="200" Height="25" Margin="10" Click="buttonClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
